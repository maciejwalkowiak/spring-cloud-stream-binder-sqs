# Spring Cloud Stream Binder SQS Sample - Simple Producer

How to produce a message using `Source` and send it to SQS queue. Includes samples for sending `plain/text` and `application/json` content types. 
